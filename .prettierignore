.DS_Store
node_modules
coverage

.husky
dist
build
.next

.gitignore
.git
.vscode
.idea

pnpm-lock.yaml
package-lock.json
yarn.lock
bun.lockb
# Regenerated by `next build` / `next typegen` with its own quoting
next-env.d.ts

# Local agent tooling (ignored via global gitignore, never in CI)
.playwright-cli
.serena
